<odoo>
    <record id="quality_check_action_mo" model="ir.actions.act_window">
        <field name="name">Quality Checks</field>
        <field name="res_model">quality.check</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('production_id', '=', active_id)]</field>
        <field name="context">{
            'search_default_production_id': [active_id],
            'default_production_id': active_id,
            'search_default_quality_checks': 1,
         }
        </field>
    </record>

    <record id="quality_check_view_form_inherit_mrp" model="ir.ui.view">
        <field name="name">quality.check.view.form.inherit.mrp</field>
        <field name="model">quality.check</field>
        <field name="inherit_id" ref="quality_control.quality_check_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='picking_id']" position="after">
                <field name="production_id" invisible="(quality_state in ('pass', 'fail') and not production_id) or context.get('from_barcode') or hide_production_id > 0"/>
            </xpath>
        </field>    
    </record>

    <record id="quality_point_routing_view_form_inherit_quality_mrp" model="ir.ui.view">
        <field name="name">quality.point.routing.view.form.inherit.quality.mrp</field>
        <field name="model">quality.point</field>
        <field name="inherit_id" ref="mrp_workorder.quality_point_routing_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='measure_on']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//field[@name='measure_frequency_type']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
        </field>
    </record>
</odoo>
